# 實作規格範例

> 以下是幾個高優先級工具的詳細實作規格

---

## 1. 擲硬幣 (Coin Flip)

### 功能需求
- 點擊按鈕隨機顯示正面或反面
- 動畫效果 (硬幣翻轉)
- 統計功能 (正面/反面次數)
- 重置按鈕

### UI 設計
```
┌─────────────────────────┐
│     🪙 擲硬幣工具      │
├─────────────────────────┤
│                         │
│      [硬幣圖示]         │
│        正面             │
│                         │
│   [   擲硬幣   ]       │
│   [   重置     ]       │
│                         │
│  統計:                  │
│  正面: 5 次 (50%)      │
│  反面: 5 次 (50%)      │
└─────────────────────────┘
```

### 技術實作
```typescript
// 組件結構
CoinFlip.tsx
├─ useState: result (heads/tails)
├─ useState: isFlipping (boolean)
├─ useState: stats ({ heads: 0, tails: 0 })
├─ flipCoin(): void
├─ reset(): void
└─ CSS animations

// 動畫
@keyframes flip {
  0% { transform: rotateY(0deg); }
  100% { transform: rotateY(1800deg); }
}
```

### i18n Keys
```typescript
{
  tool_coinflip: "擲硬幣",
  tool_coinflip_desc: "隨機擲硬幣,顯示正面或反面",
  coinflip_heads: "正面",
  coinflip_tails: "反面",
  coinflip_flip: "擲硬幣",
  coinflip_reset: "重置",
  coinflip_stats: "統計"
}
```

---

## 2. 擲骰子 (Dice Roller)

### 功能需求
- 可選擇骰子數量 (1-6個)
- 動畫效果 (骰子滾動)
- 顯示總和
- 歷史紀錄 (最近10次)

### UI 設計
```
┌─────────────────────────┐
│     🎲 擲骰子工具      │
├─────────────────────────┤
│ 骰子數量: [1][2][3]... │
│                         │
│  🎲  🎲  🎲            │
│   4    6    2           │
│                         │
│  總和: 12               │
│  [   擲骰子   ]        │
│                         │
│  歷史紀錄:              │
│  12, 8, 15, 6, 9...    │
└─────────────────────────┘
```

### 技術實作
```typescript
interface DiceResult {
  dice: number[]
  sum: number
  timestamp: number
}

// 組件結構
DiceRoller.tsx
├─ useState: diceCount (1-6)
├─ useState: results (number[])
├─ useState: isRolling (boolean)
├─ useState: history (DiceResult[])
├─ rollDice(): void
└─ 3D CSS transforms
```

---

## 3. 輪盤抽獎 (Wheel Spinner)

### 功能需求
- 自訂選項 (文字輸入,逐行)
- 旋轉動畫 (緩動效果)
- 隨機結果
- 聲音效果 (可選)
- 自訂顏色

### UI 設計
```
┌─────────────────────────┐
│    🎯 輪盤抽獎工具     │
├─────────────────────────┤
│ 輸入選項 (每行一個):   │
│ ┌─────────────────────┐ │
│ │ 選項1               │ │
│ │ 選項2               │ │
│ │ 選項3               │ │
│ └─────────────────────┘ │
│                         │
│      [輪盤圖示]         │
│                         │
│   [   開始抽獎   ]     │
│                         │
│  結果: 選項2            │
└─────────────────────────┘
```

### 技術實作
```typescript
// 輪盤繪製使用 Canvas 或 SVG
interface WheelOption {
  id: string
  text: string
  color: string
}

WheelSpinner.tsx
├─ useState: options (WheelOption[])
├─ useState: isSpinning (boolean)
├─ useState: result (string | null)
├─ useRef: canvasRef
├─ drawWheel(): void
├─ spin(): void - 計算隨機角度
└─ easeOut 動畫函數
```

---

## 4. 計數器 (Counter)

### 功能需求
- 單一計數器: +1, -1, 重置
- 進階功能:
  - 多計數器支援
  - 自訂步進值
  - 設定目標值 (達成提示)
  - 儲存到 LocalStorage

### UI 設計
```
┌─────────────────────────┐
│     ➕ 計數器工具      │
├─────────────────────────┤
│                         │
│         42              │
│                         │
│  [ - ]  [ + ]  [重置]  │
│                         │
│  步進值: [1][5][10]    │
│  目標: 100 (42%)       │
│                         │
│  [ + 新增計數器 ]      │
└─────────────────────────┘
```

### 技術實作
```typescript
interface Counter {
  id: string
  name: string
  value: number
  step: number
  target?: number
}

Counter.tsx
├─ useState: counters (Counter[])
├─ increment(id): void
├─ decrement(id): void
├─ reset(id): void
├─ setStep(id, step): void
└─ useLocalStorage hook
```

---

## 5. Instagram 字體產生器

### 功能需求
- 輸入文字
- 即時預覽多種字體樣式
- 一鍵複製
- 支援表情符號

### UI 設計
```
┌─────────────────────────┐
│  ✨ Instagram字體產生  │
├─────────────────────────┤
│ 輸入文字:               │
│ ┌─────────────────────┐ │
│ │ Hello World         │ │
│ └─────────────────────┘ │
│                         │
│ 𝓗𝓮𝓵𝓵𝓸 𝓦𝓸𝓻𝓵𝓭    [複製]│
│ ℍ𝕖𝕝𝕝𝕠 𝕎𝕠𝕣𝕝𝕕    [複製]│
│ 𝐇𝐞𝐥𝐥𝐨 𝐖𝐨𝐫𝐥𝐝    [複製]│
│ 𝙷𝚎𝚕𝚕𝚘 𝚆𝚘𝚛𝚕𝚍    [複製]│
│ 🅷🅴🅻🅻🅾 🆆🅾🆁🅻🅳    [複製]│
└─────────────────────────┘
```

### 技術實作
```typescript
// Unicode 字體映射表
const fonts = {
  bold: { a: '𝐚', b: '𝐛', ... },
  italic: { a: '𝘢', b: '𝘣', ... },
  boldItalic: { a: '𝙖', b: '𝙗', ... },
  script: { a: '𝒶', b: '𝒷', ... },
  // ... more fonts
}

function convertText(text: string, font: FontMap): string {
  return text.split('').map(char =>
    font[char.toLowerCase()] || char
  ).join('')
}

InstagramFonts.tsx
├─ useState: inputText
├─ convertText(text, fontMap)
├─ copyToClipboard(text)
└─ Font mappings
```

---

## 6. 線上尺規 (Online Ruler)

### 功能需求
- 切換公分/英吋
- 校正功能 (讓用戶輸入已知物品尺寸)
- 水平/垂直模式
- 深色/淺色主題

### UI 設計
```
┌─────────────────────────┐
│     📏 線上尺規        │
├─────────────────────────┤
│ 單位: [公分] [英吋]    │
│ 方向: [水平] [垂直]    │
│                         │
│  [校正尺規]             │
│                         │
├─────────────────────────┤
│ 0  1  2  3  4  5  6  7  │
│ |  |  |  |  |  |  |  |  │
│                         │
└─────────────────────────┘
```

### 技術實作
```typescript
OnlineRuler.tsx
├─ useState: unit ('cm' | 'inch')
├─ useState: orientation ('horizontal' | 'vertical')
├─ useState: pixelsPerCm (number)
├─ calibrate() - 校正 DPI
├─ calculatePixelsPerUnit()
└─ CSS: 1cm = 96px / 2.54

// 校正流程
1. 偵測螢幕 DPI
2. 讓用戶放置實體物品對比
3. 調整比例
4. 儲存到 localStorage
```

---

## 7. 計分板 (Scoreboard)

### 功能需求
- 多玩家支援 (2-8人)
- 加減分數
- 重置單人/全部
- 歷史紀錄
- 響應式設計

### UI 設計
```
┌─────────────────────────┐
│     🏆 計分板          │
├─────────────────────────┤
│ [ + 新增玩家 ]         │
│                         │
│ ┌─────────┬─────────┐  │
│ │ 玩家1   │ 玩家2   │  │
│ │   15    │   12    │  │
│ │ [-][+]  │ [-][+]  │  │
│ └─────────┴─────────┘  │
│                         │
│  [重置全部]  [歷史]    │
└─────────────────────────┘
```

### 技術實作
```typescript
interface Player {
  id: string
  name: string
  score: number
  history: number[]
}

Scoreboard.tsx
├─ useState: players (Player[])
├─ addPlayer(name): void
├─ updateScore(id, delta): void
├─ resetPlayer(id): void
├─ resetAll(): void
└─ useLocalStorage for persistence
```

---

## 實作檢查清單

每個工具實作時請確認:

### ✅ 功能完整性
- [ ] 核心功能正常運作
- [ ] 邊界條件處理
- [ ] 錯誤處理
- [ ] 輸入驗證

### 🎨 UI/UX
- [ ] 響應式設計 (手機/平板/桌面)
- [ ] 深色模式支援
- [ ] Loading 狀態
- [ ] 動畫流暢

### 🌏 國際化
- [ ] i18n keys 完整
- [ ] 繁體中文翻譯
- [ ] 英文翻譯

### ♿ 無障礙
- [ ] 鍵盤操作支援
- [ ] ARIA 標籤
- [ ] 色彩對比度

### 📱 PWA
- [ ] 離線使用
- [ ] LocalStorage 儲存

### 🧪 測試
- [ ] 單元測試
- [ ] 手動測試各種情境

---

**參考資源**:
- Unicode 字體: https://unicode-table.com/
- Canvas 教學: https://developer.mozilla.org/en-US/docs/Web/API/Canvas_API
- Web Animations API: https://developer.mozilla.org/en-US/docs/Web/API/Web_Animations_API
